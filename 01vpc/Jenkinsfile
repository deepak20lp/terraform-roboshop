pipeline {
    agent {node {label "AGENT-1"}}

    stages {
        stage('init') {
            steps {
                sh '''
                    cd 01vpc
                    terraform init
                '''
            }
        }
        stage('plan') {
            steps {
                echo 'planning'
                sh 'terraform plan' 
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}